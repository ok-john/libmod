name: ca

on:
  workflow_dispatch:
  push:
    branch: [ main ]
  pull_request:
    types: [ assigned, opened, synchronize, reopened ]

jobs:
  build:
    runs-on: ubuntu-latest
    - name: create-draft
      run: |
        make new-ca
