name: make

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: make all
        run: |
          sudo apt install make
          sudo make trace
          sudo make first-install
